version: '3.9'
services:
  web:
    build: .
    ports:
      - "5173:80"
    environment:
      - VITE_OPENPROJECT_BASE_URL=${VITE_OPENPROJECT_BASE_URL:-https://openproject.example}
      - VITE_OPENPROJECT_TOKEN=${VITE_OPENPROJECT_TOKEN:-replace-me}
    restart: unless-stopped
